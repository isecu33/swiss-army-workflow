# hooks/

Los hooks activos se configuran en `settings.template.json` (Stop: cost-tracker,
session-save) y los scripts viven en `scripts/`. Este directorio queda reservado
para definiciones de hooks adicionales del equipo.
